// 터치 동작에 따라 Event를 발생시킬 수 있는 라이브러리 추가 여부 고민
import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent('main', () => App);
